import * as React from "react";
import Calculator from './containers/calculator'

export const App = (): JSX.Element => {
  return (
    <div>
      <Calculator />
    </div>
  );
};
export default App;
